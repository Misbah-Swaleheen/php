<?php 
    $var = ['1996-08-15','1996-08-15','1996-08-15'];
    // $convert = strtotime($var[0]);
    // $new_format = date('Y-m-d', $convert);
    echo $new_format;
    foreach($arr as $item){
        // $convert = strtotime($var);
        // $new_format = date('Y-m-d', $convert);
        // $new_format = DateTime::createFromFormat('Y-m-d', $item)->format('Y-m-d');
        // echo $new_format;
       
        echo "\n";
    }
?>